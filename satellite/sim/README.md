# Simulation

The current simulator runs a fixed command sequence in `src/main.c`.

Future extensions can read `commands.txt` and feed each command through `command_parse`.

Command words:

- `safe`
- `nominal`
- `science`
- `downlink`
- `stop_payload`
